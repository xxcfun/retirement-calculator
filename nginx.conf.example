server {
  listen 80;
  server_name example.com;
  root /var/www/retirement-calculator-h5/dist;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location ~* \.(js|css|png|jpg|jpeg|svg|woff2)$ {
    expires 7d;
    add_header Cache-Control "public, immutable";
  }
}
